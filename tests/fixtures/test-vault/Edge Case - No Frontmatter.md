# Note Without Frontmatter

This note has no YAML frontmatter at all. Just pure markdown content.

It still has some WikiLinks like [[Photography Equipment]] and [[Sarah Chen]].

And inline tags: #test #edge-case #no-metadata

This tests the parser's ability to handle notes without frontmatter gracefully.
