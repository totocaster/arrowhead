# Arrowhead Guide

- Prefer ISO-dated fleeting notes (`2024-01-15-F123000`) for quick capture.
- Store attachments inside `Attachments/` to keep root tidy.
- Use [[wikilinks]] for internal references and keep metadata in YAML front matter.
